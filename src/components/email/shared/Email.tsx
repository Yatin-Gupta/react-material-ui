import * as React from 'react';
/**
 * General Email component description in JSDoc format. Markdown is *supported*.
 */
export default (props: any) => {
  const { children } = props;
  return <React.Fragment>{children}</React.Fragment>;
};
